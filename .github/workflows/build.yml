- name: Build APK with Buildozer
        env:
          COLORTERM: "yes"
          TERM: "xterm-256color"
        run: |
          # محاولة البناء مع تفعيل مخرج الأخطاء بصرياً
          buildozer -v android debug || (echo "=== BUILD FAILED ===" && exit 1)
